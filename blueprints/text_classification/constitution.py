# /home/zhz/zhzai/model_foundry/scripts/prompt_constitutions.py
# 编码: UTF-8
# 功能: 定义所有用于数据生成的“宪法”级Prompt。
# 这是一个中心化的Prompt库，旨在与数据生成逻辑解耦。

def IS_QUESTION_CONSTITUTION(samples_per_request: int) -> str:
    """
    为 IsQuestionClassifier 生成数据的“宪法”。
    该“宪法”经过精心设计，以消除歧义，确保生成高质量、一致性的训练数据。
    """
    return f"""
你是一位为机器学习分类器生成训练数据的专家。你的任务是为个人AI助手生成一系列多样化的用户输入文本。
对于每一条文本，你必须为其分配一个分类标签：“Question”（信息征询）或“Statement”（指令与陈述）。

**核心指导思想：**
分类的唯一目的是区分用户的意图：用户是想“从AI获取信息”，还是想“让AI执行动作或记录信息”。

---
### **标签定义: `Question` (信息征询)**

**核心哲学：** 用户的知识状态存在“缺口”，他们期望通过与AI交互来**填补**这个缺口。AI的角色是**信息提供者**。
**黄金法则：** 如果AI收到输入后，其最主要的下一步是**查询知识库、访问API获取数据、或进行计算，然后将结果呈现给用户**，那么这个输入就是 `Question`。

**包含类别：**
1.  **标准的“5W1H”问句：** (例如: "珠穆朗玛峰有多高？", "上次世界杯是什么时候举办的？")
2.  **是非与选择问句：** (例如: "你能够连接到互联网吗？", "咖啡和茶，哪个咖啡因含量更高？")
3.  **祈使句形式的知识请求：** (例如: "给我讲讲人工智能的历史。", "解释一下什么是区块链。", "介绍一下你自己。")
4.  **无疑问词的口语化提问：** (例如: "我下一个日程安排。", "我的IP地址。", "你叫什么名字。")
5.  **计算与转换请求：** (例如: "计算一下1024乘以768。", "500美元等于多少人民币？")

---
### **标签定义: `Statement` (指令与陈述)**

**核心哲学：** 用户期望AI**改变世界的状态**（如设置闹钟）、**记录新的信息**，或者用户只是在发表一个**无需AI回应具体信息的观点或事实**。AI的角色是**执行者**或**记录员**。
**黄金法则：** 如果AI收到输入后，其最主要的下一步是**调用一个工具（如日历）、执行一个动作（如搜索）、或将信息存入数据库**，那么这个输入就是 `Statement`。

**包含类别：**
1.  **明确的指令与命令：** (例如: "提醒我下午三点开会。", "播放一首舒缓的音乐。", "把屏幕亮度调到80%。")
2.  **搜索指令（关键界定）：** (例如: "搜索一下最近的科技新闻。", "帮我查一下去北京的机票。", "在网上找一下这个问题的解决方案。")
3.  **信息记录与存储：** (例如: "帮我记一下，我太太的生日是10月25日。", "把这个想法存到我的点子库。")
4.  **事实陈述与个人观点：** (例如: "今天天气真不错。", "我觉得这个新功能很有用。")
5.  **简单的对话回应与确认：** (例如: "好的，明白了。", "收到。", "没问题。")

---
**核心指令:** 
1.  **精确生成 {samples_per_request} 条**独一无二的示例。
2.  输出**必须是**一个严格合法的JSON数组，数组中的每个对象都包含 "text" 和 "label" 两个键。
3.  除了这个JSON数组，**不要包含任何**额外的解释、Markdown标记或其他任何文字。
4.  确保“Question”和“Statement”两种标签的数量大致均衡。
5.  所有生成的文本都必须是**中文简体**。
"""

def CONFIRMATION_CONSTITUTION(samples_per_request: int) -> str:
    """
    为 ConfirmationClassifier 生成数据的“宪法”。
    """
    return f"""
你是一位为机器学习分类器生成训练数据的专家。
场景是：一个AI助手向用户提出了一个确认请求（例如：“您确定要删除这条记忆吗？”）。
你的任务是生成一系列用户可能会如何回复的、多样化的例子。
对于每一条回复，你必须为其分配一个分类标签：“Affirm”（肯定）或“Deny”（否定）。

**核心指导思想：**
分类的目的是判断用户的回应是否在**推进**或**中止**AI助手提出的待定动作。

---
### **标签定义: `Affirm` (肯定)**

**核心哲学：** 用户的回应明确表示同意、确认或允许AI继续执行待定动作。
**包含类别：**
1.  **直接肯定：** (例如: "是的", "没错", "可以")
2.  **英文肯定：** (例如: "ok", "yes", "sure", "yep")
3.  **行动性肯定：** (例如: "继续吧", "执行操作", "就这么办")
4.  **带条件的肯定：** (例如: "是的，但把它改成明天下午")

---
### **标签定义: `Deny` (否定)**

**核心哲学：** 用户的回应明确表示不同意、取消、否定待定动作，或给出了一个完全不相关的答复，从而中止当前流程。
**包含类别：**
1.  **直接否定：** (例如: "不", "别", "取消", "不对")
2.  **犹豫或不确定：** (例如: "让我想想", "我再看看", "我不知道")
3.  **无关回复：** (例如: "今天天气怎么样？", "你叫什么名字")
4.  **反问：** (例如: "为什么要删除它？", "有什么影响吗？")

---
**核心指令:**
1.  **精确生成 {samples_per_request} 条**独一无二的示例。
2.  输出**必须是**一个严格合法的JSON数组，数组中的每个对象都包含 "text" 和 "label" 两个键。
3.  除了这个JSON数组，**不要包含任何**额外的解释、Markdown标记或其他任何文字。
4.  确保“Affirm”和“Deny”两种标签的数量大致均衡。
5.  所有生成的文本都必须是**中文简体**。
"""